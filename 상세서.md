# 거래명세서 자동 생성 프로그램 UI/UX 상세서 (Windows Desktop App)

> 적용 스킬: `ui-ux-pro-max`(접근성/상태/레이아웃/모션 기준), `writing-clearly-and-concisely`(규칙 중심 문장), `prompt-engineering`(표/토큰/상태 템플릿화)

---

## 0) 문서 정보

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 문서명 | 거래명세서 자동 생성 프로그램 UI/UX 상세서 | 전체 | 본 문서를 그대로 `상세서.md` 본문으로 사용 |
| 버전 | v1.0 | 전체 | UI 구현 기준선(Design Baseline) |
| 대상 OS | Windows 10/11 | 전체 | 마우스+키보드 기본, 터치(선택) 고려 |
| 기본 테마 | Light only | 전체 | 다크모드는 v2 범위로 분리(가정 참조) |
| 기준 DPI/스케일 | 96DPI / 100% | 전체 | 125%에서도 깨짐/겹침 없어야 함(UI QA 포함) |
| 최소 창 크기 | 1120×720px | 전체 | 이보다 작으면 스크롤/패널 접힘 규칙 적용 |
| 표기 단위 | px / ms / HEX | 전체 | 예외: 폰트 패밀리는 문자열로 표기 |

---

## 1) UX 목표 및 원칙(규칙)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 정확성 우선 | 금액 오차 0원 | 처리/검증 | 계산 결과가 확정되기 전 “미리보기(검증)” 단계 제공 |
| 오류 예방 | 필수값 누락 즉시 감지 | 입력/마법사 | 제출 전 블로킹(필수값) + 제출 후 요약(전체 오류) 동시 제공 |
| 대량 처리 | 50파일/5분 목표 | 일괄 처리 | UI는 비동기 작업(중단/재시도/계속) 전제로 설계 |
| 가시성 | 진행률/상태 항상 노출 | 일괄 처리 | 전체 진행률 + 파일별 상태(성공/경고/실패) 2단 구조 |
| 복구 가능 | 실패해도 나머지 진행 | 일괄 처리 | “실패 항목만 재시도” 버튼 제공, 실패 사유 1클릭 확인 |
| 비개발자 친화 | 용어/레이블 표준화 | 전체 | 기술 용어 노출 금지(예: “파서”) → “발주서 읽기 규칙” |

---

## 2) 정보 구조(IA) 및 네비게이션

### 2-1. 좌측 사이드바 메뉴(기본)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 메뉴 구조 | 6개 상위 메뉴 | App Shell | `일괄 처리 / 업체 관리 / 매핑 마법사 / 단가·규칙 / 로그·리포트 / 설정` |
| 기본 진입 화면 | 일괄 처리 | 최초 실행 | 사용자의 일일 업무 흐름과 일치 |
| 메뉴 라벨 길이 | 최대 10자 권장 | 사이드바 | 넘치면 2줄 금지, 말줄임(…) 적용 |
| 검색 진입 | Ctrl+K | 전체 | 전역 검색(업체/품목/로그) 검색창 포커스 |

### 2-2. App Shell(공통 뼈대)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 헤더 높이 | 56px | 상단 | 화면 제목 + 현재 작업 컨텍스트(날짜/출력경로) |
| 사이드바 폭(기본) | 264px | 좌측 | 아이콘 20px, 텍스트 13px |
| 사이드바 폭(접힘) | 72px | 좌측 | `bp_compact`에서 기본 접힘(아이콘만) |
| 콘텐츠 패딩 | 24px | 본문 | `bp_compact`에서는 16px |
| 섹션 간 간격 | 16px | 본문 | 카드·테이블·폼 블록 간 기본 간격 |
| 우측 인스펙터(선택) | 360px | 상세패널 | `bp_wide`에서 기본 표시(로그/오류 상세) |

---

## 3) 디자인 토큰(Design Tokens)

> 토큰 이름은 구현 코드에서 상수로 그대로 사용한다(예: `COLOR_PRIMARY_600`).

### 3-1. Color (Light)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| color.bg.app | #F8FAFC | 앱 배경 | 기본 배경(스크롤 영역 포함) |
| color.bg.surface | #FFFFFF | 카드/모달/드로어 | 모든 “Surface” 기본 배경 |
| color.bg.muted | #F1F5F9 | 입력 disabled, 테이블 스트라이프 | 정보성 강조가 아닌 영역에만 사용 |
| color.text.primary | #0F172A | 본문 텍스트 | 기본 텍스트(최소 대비 4.5:1) |
| color.text.muted | #475569 | 보조 설명/메타 | 본문 대체 금지(가독성 저하) |
| color.text.placeholder | #64748B | 플레이스홀더 | 입력 안내용, 값과 혼동 없게 |
| color.border.default | #E2E8F0 | 카드/테이블/입력 | 기본 1px 보더 |
| color.border.strong | #CBD5E1 | hover/구분선 | 구분 강조가 필요할 때만 사용 |
| color.primary.50 | #EFF6FF | 선택/호버 배경 | 테이블 행 hover, 정보 배경 |
| color.primary.100 | #DBEAFE | 선택 배경 | 선택(Selected) 기본 배경 |
| color.primary.600 | #2563EB | Primary CTA | 주요 버튼/강조 링크 |
| color.primary.700 | #1D4ED8 | Hover | Primary hover |
| color.primary.800 | #1E40AF | Active | Primary active |
| color.success.600 | #16A34A | 성공 상태 | 성공 배지/토스트/아이콘 |
| color.warning.600 | #D97706 | 경고 상태 | 경고 배지/토스트 |
| color.danger.50 | #FEF2F2 | 에러 배경 | 에러 행/배너 배경 |
| color.danger.600 | #DC2626 | 오류 상태 | 오류 보더/텍스트/토스트 |
| color.focus.ring | #93C5FD | 포커스 링 | 모든 인터랙티브 요소 공통 |
| color.overlay.scrim | rgba(15,23,42,0.60) | 모달/드로어 오버레이 | 클릭 시 닫기 허용(필수 입력 모달 제외) |

### 3-2. Typography

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| font.family.base | Segoe UI, Malgun Gothic, Arial, sans-serif | 전체 | OS 기본과 조화, 폰트 강제 다운로드 금지 |
| font.size.xs | 12px | 캡션/메타 | 표 보조 정보, 힌트 |
| font.size.sm | 13px | 본문 기본 | 앱 기본 폰트 크기 |
| font.size.md | 14px | 폼 라벨/강조 | 과다 사용 금지(밀도 증가) |
| font.size.lg | 16px | 섹션 타이틀 | 카드/블록 헤더 |
| font.size.xl | 18px | 화면 타이틀 | 상단 헤더 타이틀 |
| font.weight.regular | 400 | 본문 | 기본 |
| font.weight.semibold | 600 | 버튼/제목 | 제목/주요 CTA |
| lineHeight.xs | 16px | 12px 텍스트 | 최소 1.3 이상 |
| lineHeight.sm | 20px | 13px 텍스트 | 본문 기본 |
| lineHeight.md | 22px | 14px 텍스트 | 폼 라벨/설명 |
| lineHeight.lg | 24px | 16px 텍스트 | 섹션 제목 |
| letterSpacing.default | 0px | 전체 | 임의 자간 금지(가독성 저하) |

### 3-3. Spacing

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| space.0 | 0px | 리셋 | 불필요한 0 반복 금지(토큰 사용) |
| space.1 | 4px | 미세 간격 | 아이콘-텍스트, 배지 |
| space.2 | 8px | 기본 간격 | 폼 내부 요소 |
| space.3 | 12px | 컴포넌트 패딩 | 입력/카드 내부 |
| space.4 | 16px | 섹션 간격 | 카드 간 기본 |
| space.5 | 20px | 넉넉 간격 | 대시보드 블록 |
| space.6 | 24px | 페이지 패딩 | 콘텐츠 외곽 패딩 |
| space.8 | 32px | 큰 구분 | 섹션/그룹 분리 |
| space.10 | 40px | 매우 큰 구분 | 빈 상태(Empty) 레이아웃 |

### 3-4. Radius

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| radius.sm | 6px | 인풋/배지 | 작은 요소 |
| radius.md | 10px | 버튼/토스트 | 기본 |
| radius.lg | 14px | 카드/모달 | Surface 기본 |
| radius.pill | 999px | 태그/칩 | 캡슐형 요소 |

### 3-5. Shadow (선택적)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| shadow.sm | 0 1 2 rgba(15,23,42,0.06) | 카드 hover | 과도한 입체감 금지 |
| shadow.md | 0 4 12 rgba(15,23,42,0.10) | 드로어 | 표면 분리 |
| shadow.lg | 0 12 32 rgba(15,23,42,0.14) | 모달 | 최상위 레이어 강조 |

### 3-6. Motion

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| motion.duration.fast | 120ms | hover | 색/보더 전환 |
| motion.duration.base | 200ms | 모달/드로어 | 열림/닫힘 |
| motion.duration.slow | 280ms | 큰 전환 | 화면 전환(가능 시) |
| motion.easing.standard | cubic-bezier(0.2,0,0,1) | 전체 | 기본 easing |
| motion.reduce | 0ms | 접근성 | “모션 최소화” 설정 시 적용 |

### 3-7. Z-index(레이어)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| z.base | 0 | 기본 | 콘텐츠 |
| z.sticky | 10 | 고정 헤더 | 테이블 헤더/필터바 |
| z.dropdown | 20 | 콤보박스 | 드롭다운/메뉴 |
| z.overlay | 30 | 스크림 | 모달/드로어 배경 |
| z.modal | 40 | 모달 | 최상위 입력 |
| z.toast | 50 | 토스트 | 화면 우상단 |
| z.tooltip | 60 | 툴팁 | 포인터/포커스 툴팁 |

---

## 4) 레이아웃 규칙

### 4-1. 브레이크포인트(창 너비 기준)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| bp_compact | ≤1200px | 전체 | 사이드바 기본 접힘(72px), 우측 인스펙터 숨김 |
| bp_regular | 1201~1600px | 전체 | 기본 레이아웃(사이드바 264px) |
| bp_wide | ≥1601px | 전체 | 우측 인스펙터(360px) 기본 표시 |

### 4-2. 그리드/컨테이너

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| grid.columns | 12 | 본문 | 카드/폼 레이아웃 정렬 기준 |
| grid.gutter | 24px | 본문 | 열 사이 간격 |
| container.padding | 24px | 본문 | `bp_compact`에서는 16px |
| form.label.col | 160px | 폼 | 라벨 좌측 정렬(2열 폼 기본) |
| form.field.gapY | 12px | 폼 | 필드 간 세로 간격 |

### 4-3. 스크롤/고정(Sticky) 규칙

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| sticky.filterBar | top:0 | 일괄 처리 | 필터/실행 영역은 스크롤 시 상단 고정 |
| sticky.tableHeader | top:0 | 테이블 | 헤더 고정, 본문만 스크롤 |
| max.nestedScroll | 2 | 전체 | 중첩 스크롤은 최대 2단(페이지 + 테이블) |
| inspector.scroll | 내부 스크롤 | 우측 패널 | 우측 상세는 자체 스크롤, 페이지 스크롤과 분리 |

---

## 5) 공통 상태(States) 의미 규칙

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| state.default | - | 전체 | 기본 상태 |
| state.hover | +보더 강조 | 버튼/행/카드 | 레이아웃 흔들림(크기 변화) 금지 |
| state.active | 색상 1단계 진하게 | 버튼/행 | 클릭/프레스 순간만 |
| state.focus | ring 2px | 전체 | 키보드 포커스 시 반드시 표시 |
| state.disabled | opacity 0.55 | 전체 | 클릭/입력 불가, 툴팁으로 이유(선택) |
| state.loading | 스피너 16px | 전체 | 동일 영역에서 로딩 표시, 버튼은 비활성화 |
| state.error | danger.600 | 입력/행/배너 | “색만”으로 알리지 말고 텍스트 포함 |

---

## 6) 컴포넌트 규격(필수)

> 모든 컴포넌트는 `default/hover/active/focus/disabled/loading/error` 상태를 가진다(해당 상태가 “의미 없음”인 경우에도 동작을 정의한다).

---

### 6-1. Button

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Button.height.sm | 28px | 보조 액션 | 툴바/행 내부 |
| Button.height.md | 32px | 기본 | 대부분 |
| Button.height.lg | 40px | 주요 CTA | “일괄 처리 시작” 등 |
| Button.paddingX.sm | 12px | sm | 좌우 패딩 |
| Button.paddingX.md | 14px | md | 좌우 패딩 |
| Button.paddingX.lg | 16px | lg | 좌우 패딩 |
| Button.radius | 10px | 전체 | `radius.md` 사용 |
| Button.gap | 8px | 아이콘+텍스트 | 아이콘-텍스트 간격 |
| Button.icon | 16px | 아이콘 버튼 | 아이콘 전용은 32×32px 최소 |
| Button.focusRing | 2px | focus | `color.focus.ring` |
| Button.transition | 120ms | hover/active | `motion.duration.fast` |

#### B) Primary(주요)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Button.Primary.bg.default | #2563EB | 기본 | `color.primary.600` |
| Button.Primary.bg.hover | #1D4ED8 | hover | `color.primary.700` |
| Button.Primary.bg.active | #1E40AF | active | `color.primary.800` |
| Button.Primary.text.default | #FFFFFF | 기본 | 대비 4.5:1 확보 |
| Button.Primary.focus.ring | #93C5FD / 2px | focus | 링은 밖으로(레이아웃 변화 없음) |
| Button.Primary.disabled.bg | #E2E8F0 | disabled | 클릭 불가 |
| Button.Primary.disabled.text | #64748B | disabled | 텍스트 대비 유지 |
| Button.Primary.loading | spinner 16px | loading | 버튼 비활성 + 스피너(좌측) |
| Button.Primary.error | border #DC2626 / 1px | error | 실패 직후 2000ms 표시 + 토스트 병행 |

#### C) Secondary(기본)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Button.Secondary.bg.default | #FFFFFF | 기본 | `color.bg.surface` |
| Button.Secondary.border.default | #CBD5E1 | 기본 | `color.border.strong` |
| Button.Secondary.bg.hover | #F8FAFC | hover | `color.bg.app` |
| Button.Secondary.bg.active | #F1F5F9 | active | `color.bg.muted` |
| Button.Secondary.focus.ring | #93C5FD / 2px | focus | 공통 |
| Button.Secondary.disabled | opacity 0.55 | disabled | 공통 규칙 |
| Button.Secondary.loading | spinner 16px | loading | 공통 |
| Button.Secondary.error | border #DC2626 / 1px | error | 공통 |

#### D) Danger(삭제/위험)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Button.Danger.bg.default | #DC2626 | 기본 | `color.danger.600` |
| Button.Danger.bg.hover | #B91C1C | hover | (기본값) 더 진하게 |
| Button.Danger.bg.active | #991B1B | active | (기본값) 더 진하게 |
| Button.Danger.focus.ring | #93C5FD / 2px | focus | 포커스는 동일 색(일관성) |
| Button.Danger.disabled | opacity 0.55 | disabled | 공통 |
| Button.Danger.loading | spinner 16px | loading | 공통 |
| Button.Danger.error | 유지(기본) | error | Danger 자체가 오류색이므로 별도 테두리 추가 금지 |

---

### 6-2. Input (Text Field)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Input.height.md | 32px | 기본 | 폼 기본 |
| Input.height.lg | 40px | 설정/긴 폼 | 가독성 우선 화면 |
| Input.paddingX | 12px | 전체 | 좌우 패딩 |
| Input.radius | 6px | 전체 | `radius.sm` |
| Input.border | 1px | 전체 | 보더 고정(레이아웃 흔들림 금지) |
| Input.font | 13px / 400 | 전체 | 본문과 동일 |
| Input.helper.font | 12px | 도움말 | `font.size.xs` |
| Input.error.font | 12px | 오류 | 짧고 구체적으로 |
| Input.icon | 16px | 검색/클리어 | 아이콘 버튼은 32×32px |

#### B) 상태(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Input.bg.default | #FFFFFF | default | `color.bg.surface` |
| Input.border.default | #CBD5E1 | default | `color.border.strong` |
| Input.border.hover | #94A3B8 | hover | (기본값) 1단계 진하게 |
| Input.border.active | #2563EB | active | 클릭 순간(선택) |
| Input.border.focus | #2563EB | focus | 포커스 시 보더=primary |
| Input.focus.ring | #93C5FD / 2px | focus | 공통 |
| Input.disabled.bg | #F1F5F9 | disabled | `color.bg.muted` |
| Input.disabled.text | #475569 | disabled | 값은 읽히게(완전 회색 금지) |
| Input.loading | spinner 16px | loading | 우측에 스피너, 입력 잠금 |
| Input.error.border | #DC2626 | error | `color.danger.600` |
| Input.error.helper | #DC2626 | error | 오류 텍스트는 입력 하단 1줄 |

---

### 6-3. Textarea

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Textarea.minHeight | 96px | 비고/규칙 편집 | 3줄 기본 |
| Textarea.maxHeight | 240px | 설정 | 그 이상은 내부 스크롤 |
| Textarea.padding | 12px | 전체 | 상하좌우 동일 |
| Textarea.resize | vertical only | 전체 | 가로 리사이즈 금지(레이아웃 붕괴) |

#### B) 상태(필수: Input과 동일)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Textarea.default | Input 동일 | default | 스타일/보더/포커스 규칙 동일 |
| Textarea.hover | Input 동일 | hover | 동일 |
| Textarea.active | Input 동일 | active | 동일 |
| Textarea.focus | Input 동일 | focus | 동일 |
| Textarea.disabled | Input 동일 | disabled | 동일 |
| Textarea.loading | spinner 16px | loading | 우측 상단에 표시 |
| Textarea.error | danger.600 | error | 보더+하단 메시지 |

---

### 6-4. Card

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Card.bg | #FFFFFF | 카드 | `color.bg.surface` |
| Card.border | 1px / #E2E8F0 | 카드 | `color.border.default` |
| Card.radius | 14px | 카드 | `radius.lg` |
| Card.padding | 16px | 카드 본문 | `space.4` |
| Card.header.height | 48px | 카드 헤더 | 타이틀+액션(우측) |
| Card.gap | 12px | 내부 | 섹션 간 간격 |

#### B) 상태(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Card.default | border #E2E8F0 | default | 기본 |
| Card.hover | border #CBD5E1 + shadow.sm | hover | 클릭 가능한 카드에만 적용 |
| Card.active | border #CBD5E1 | active | 그림자 약화(선택) |
| Card.focus | ring #93C5FD / 2px | focus | 카드가 클릭 타깃일 때만 |
| Card.disabled | opacity 0.55 | disabled | 클릭/입력 금지 |
| Card.loading | skeleton | loading | 텍스트/표 자리 고정, 점멸 금지 |
| Card.error | leftBorder 4px / #DC2626 | error | 카드 내 오류 요약이 있을 때 |

---

### 6-5. Modal

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Modal.scrim | rgba(15,23,42,0.60) | 오버레이 | 클릭 닫기 기본 ON(필수 입력 모달 OFF) |
| Modal.radius | 14px | 컨테이너 | `radius.lg` |
| Modal.shadow | shadow.lg | 컨테이너 | 최상위 강조 |
| Modal.padding | 24px | 본문 | `space.6` |
| Modal.header.height | 56px | 헤더 | 제목(18px) + 닫기 |
| Modal.footer.height | 64px | 푸터 | 버튼 정렬(우측) |
| Modal.width.sm | 480px | 단순 확인 | 확인/경고 |
| Modal.width.md | 640px | 폼 | 설정/추가 |
| Modal.width.lg | 840px | 복잡 폼 | 규칙 편집/미리보기 |

#### B) 상태(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Modal.default | open | default | 열릴 때 포커스는 첫 입력으로 이동 |
| Modal.hover | close hover 배경 #F1F5F9 | hover | 닫기 버튼에만 적용 |
| Modal.active | close active 배경 #E2E8F0 | active | 동일 |
| Modal.focus | focus trap | focus | Tab이 모달 밖으로 나가지 않게 |
| Modal.disabled | overlay click off | disabled | 처리 중(예: 저장)에는 닫기 비활성 |
| Modal.loading | skeleton + 버튼 disabled | loading | 저장/검증 중 표시 |
| Modal.error | 상단 에러 배너 | error | 오류는 모달 상단 1줄 요약 + 상세(접기) |

---

### 6-6. Drawer (우측 슬라이드 패널)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Drawer.position | right | 기본 | 우측 고정(일관성) |
| Drawer.width | 420px | 상세 | `bp_compact`에서는 100% 모달 대체 |
| Drawer.scrim | rgba(15,23,42,0.40) | 오버레이 | 모달보다 약하게 |
| Drawer.shadow | shadow.md | 패널 | 본문과 분리 |
| Drawer.header.height | 56px | 헤더 | 제목 + 닫기 |
| Drawer.padding | 16px | 본문 | `space.4` |

#### B) 상태(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Drawer.default | open | default | 열릴 때 헤더에 포커스(스크린리더 대비) |
| Drawer.hover | 닫기 버튼 hover | hover | 모달과 동일 |
| Drawer.active | 닫기 버튼 active | active | 모달과 동일 |
| Drawer.focus | 포커스 트랩(선택) | focus | 편집 드로어는 트랩 ON |
| Drawer.disabled | 닫기 비활성 | disabled | 저장/처리 중 |
| Drawer.loading | skeleton | loading | 상세 로딩 |
| Drawer.error | 에러 배너 | error | 오류 요약 + 재시도 버튼 |

---

### 6-7. Toast (알림)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Toast.width | 360px | 우상단 | 긴 메시지는 2줄까지, 이후 말줄임 |
| Toast.padding | 12px | 내부 | `space.3` |
| Toast.radius | 10px | 컨테이너 | `radius.md` |
| Toast.gap | 8px | 아이콘/텍스트 | 간격 |
| Toast.stack.gap | 8px | 여러 개 | 토스트 간 간격 |
| Toast.position | top-right / 16px | 전역 | 화면 우상단 고정 |
| Toast.duration.info | 4000ms | 정보 | 자동 닫힘 |
| Toast.duration.success | 4000ms | 성공 | 자동 닫힘 |
| Toast.duration.warning | 6000ms | 경고 | 자동 닫힘 |
| Toast.duration.error | 0ms | 오류 | 자동 닫힘 금지(사용자 닫기) |

#### B) 상태(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Toast.default | bg #FFFFFF | default | Surface 스타일 |
| Toast.hover | 타이머 일시정지 | hover | hover 시 자동 닫힘 카운트 멈춤 |
| Toast.active | 클릭 시 상세 열기 | active | 오류 토스트는 클릭→오류 패널(드로어) |
| Toast.focus | 닫기 버튼 포커스 링 | focus | 키보드로 닫기 가능 |
| Toast.disabled | 사용 안 함 | disabled | 토스트 자체는 disabled 상태 생성 금지(대신 숨김) |
| Toast.loading | 사용 안 함 | loading | 로딩은 토스트 대신 인라인/버튼 스피너 |
| Toast.error | 아이콘/텍스트 danger | error | 오류는 `color.danger.600` 사용 |

---

### 6-8. Table (데이터 테이블)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Table.header.height | 40px | 헤더 | 헤더는 sticky |
| Table.row.height | 36px | 본문 | 기본 밀도 |
| Table.row.height.compact | 32px | 대량 목록 | 화면 정보량 우선 시 |
| Table.cell.paddingX | 12px | 셀 | 좌우 패딩 |
| Table.cell.paddingY | 8px | 셀 | 상하 패딩 |
| Table.border | 1px / #E2E8F0 | 전체 | 기본 보더 |
| Table.zebra.bg | #F8FAFC | 스트라이프 | 2행마다(선택) |
| Table.text | 13px | 본문 | 기본 |

#### B) 상태(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Table.default | bg #FFFFFF | default | 기본 |
| Table.hover | row bg #EFF6FF | hover | 행 hover(선택보다 약하게) |
| Table.active | row bg #DBEAFE | active | 클릭/선택 순간 |
| Table.focus | cell ring #93C5FD / 2px | focus | 키보드 탐색 시 셀 단위 포커스 |
| Table.disabled | opacity 0.55 | disabled | 편집 불가 테이블 |
| Table.loading | skeleton rows | loading | 행 수는 고정(레이아웃 점프 금지) |
| Table.error | row bg #FEF2F2 + left 4px #DC2626 | error | 오류 행 강조 + 메시지 아이콘 |

#### C) 필수 기능 규칙(일반)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Table.sortIcon | 16px | 헤더 | 정렬 가능 컬럼만 표시 |
| Table.column.minWidth | 80px | 컬럼 | 너무 좁으면 자동 줄바꿈 금지(말줄임) |
| Table.column.ellipsis | true | 긴 텍스트 | 파일명/업체명은 말줄임 + 툴팁 |
| Table.selection | row | 목록 | 기본은 행 단위 선택 |
| Table.bulkActions | top bar | 목록 | 다중 선택 시 상단 액션 바 노출 |
| Table.emptyState | 1 카드 | 없음 | “데이터 없음” + 다음 행동 버튼 제공 |

---

## 7) 화면별 UI 구성(구현 기준)

### 7-1. 일괄 처리(대시보드)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Dashboard.filterBar.height | 56px | 상단 | 날짜 선택 + 입력폴더 + 출력폴더 + `처리 시작` |
| Dashboard.primaryCTA | Button.lg | 상단 | 기본 라벨: `일괄 처리 시작` |
| Dashboard.progressBar.height | 8px | 상단/카드 | 전체 진행률(%) 표시 |
| Dashboard.summary.card | Card | 상단 | 성공/경고/실패 건수 + 금액합계 |
| Dashboard.table.columns | 7 | 목록 | 상태/업체/파일명/문서번호/금액/저장경로/결과 |
| Dashboard.rowActions | 아이콘 16px | 목록 | 보기(미리보기)/오류보기/재시도 |
| Dashboard.failPolicy | continue | 처리 | 실패해도 다음 파일 계속 |
| Dashboard.cancel | Button.Secondary | 처리 | “전체 중단”은 확인 모달 필수 |

### 7-2. 업체 관리(리스트+상세)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| VendorList.search | Input.md | 상단 | placeholder: `업체명/코드 검색` |
| VendorList.table | Table | 좌측 | 200개 업체도 스크롤/검색 즉시 반응 |
| VendorDetail.open | Drawer(420px) | 우측 | 목록 유지한 채 상세 편집 |
| VendorDetail.sections | Tabs(권장) | 상세 | `기본/단가/매핑/생성규칙/예외키워드/OCR` |
| Vendor.save | Button.Primary | 상세 | 저장 중(loading) 닫기 disabled |
| Vendor.validation | inline error | 상세 | 필수값: 업체명/발행번호 포맷/저장경로 |

### 7-3. 매핑 설정 마법사(Wizard)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Wizard.layout | 좌:단계 / 우:내용 | 마법사 | 좌측 단계폭 240px, 우측 폼 |
| Wizard.stepper.itemHeight | 40px | 단계 | 현재 단계는 `primary.50` 배경 |
| Wizard.footer.height | 72px | 하단 | `이전/다음/저장` 고정 |
| Wizard.sampleUpload | Drag&Drop + 버튼 | 1단계 | 지원: xlsx/xls/jpg/png |
| Wizard.mappingTable | Table | 매핑 | 필드(품명/규격/수량/…) 행 기준 매핑 |
| Wizard.preview | Modal.lg | 검증 | “거래명세서 미리보기”에서 오류 강조 |
| Wizard.errorBlocking | true | 저장 | 오류 1개라도 있으면 저장 불가 |

### 7-4. 로그·리포트

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Report.dateRange | 2×Date Input | 상단 | 기본: 오늘(또는 마지막 처리일) |
| Report.export | Button.Secondary | 상단 | `엑셀 다운로드` |
| Report.table | Table | 본문 | 실행ID/일시/성공/실패/금액합계 |
| Report.detail | Drawer | 상세 | 실패 사유/파일 리스트/재시도 링크 |

### 7-5. 설정

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| Settings.path | Input + Browse | 저장경로 | OS 파일/폴더 선택 다이얼로그 사용 |
| Settings.ocrKey | Input(password) | OCR | 키는 마스킹, “테스트” 버튼 제공 |
| Settings.motionReduce | Toggle | 접근성 | ON이면 `motion.reduce` 적용 |
| Settings.reset | Button.Danger | 전체 | 초기화는 확인 모달 필수 |

---

## 8) 접근성(A11y) 체크리스트(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 색 대비 | 4.5:1 이상 | 전체 | `text.primary` 기준, muted 남용 금지 |
| 포커스 표시 | ring 2px #93C5FD | 전체 | 키보드 탐색 시 항상 보이게 |
| 키보드 내비게이션 | Tab/Shift+Tab | 전체 | 시각적 순서=탭 순서 |
| 단축키 | Ctrl+K/Ctrl+F/Esc | 전체 | Esc는 모달/드로어 닫기(처리 중이면 비활성) |
| 최소 클릭 영역 | 32×32px(권장 40×40px) | 버튼/아이콘 | 아이콘 버튼은 패딩으로 확보 |
| 오류 안내 | 텍스트+색 | 입력/테이블 | “빨간색만” 금지, 오류 문장 제공 |
| 상태 전달 | 아이콘+라벨 | 상태 배지 | 성공/경고/실패는 텍스트 라벨 포함 |
| 모션 최소화 | 0ms | 전체 | 설정 ON 시 페이드/슬라이드 제거 |
| 스크롤 접근 | PageUp/Down | 테이블 | 키보드로 목록 이동 가능해야 함 |

---

## 9) UI QA 체크리스트(필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 상태 커버리지 | 7 states | 모든 컴포넌트 | default/hover/active/focus/disabled/loading/error 확인 |
| DPI 125% | 깨짐 0건 | 전체 | 텍스트 겹침/버튼 잘림/테이블 헤더 틀어짐 금지 |
| 긴 텍스트 | 말줄임+툴팁 | 테이블 | 파일명/경로/업체명 |
| 로딩 동작 | UI 멈춤 0건 | 일괄 처리 | 처리 중 UI 프리즈 금지(진행률 갱신) |
| 실패 지속 처리 | 계속 진행 | 일괄 처리 | 1개 실패해도 나머지 완료 |
| 재시도 UX | 1클릭 재시도 | 일괄 처리/로그 | 실패 건만 재시도, 결과 갱신 |
| 저장/취소 | 중복 저장 0 | 설정/업체 | 저장 중 버튼 disabled + 로딩 표시 |
| 경로/파일 권한 | 오류 안내 | 설정/처리 | 권한/잠금 파일은 원인+해결 안내 |
| 오프라인 | OCR 관련 안내 | 설정 | OCR 사용 시 네트워크 필요 문구 |

---

## 10) 가정(Assumptions)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| UI 프레임워크 | PyQt(권장) 또는 동등 GUI | 전체 | 토큰/규격은 프레임워크 중립(픽셀 기준) |
| 다크모드 | 미지원(v1) | 전체 | 필요 시 토큰 2세트로 확장 |
| 아이콘 세트 | 단일 SVG 세트 | 전체 | 이모지 아이콘 금지 |
| 폰트 | 시스템 기본 | 전체 | Windows 기본 폰트 우선 |
| 테이블 편집 | 인라인 편집(선택) | 단가/규칙 | v1에서는 모달/드로어 편집도 허용 |
| 토스트 오류 자동닫힘 | 0ms(수동) | 오류 | 중요 오류는 사용자가 인지할 때까지 유지 |
